# Password Generator

A secure, client-side password generator built with React. Generate strong passwords instantly with customizable options — no tracking, no storage, fully private.

## Features

- Generate secure random passwords
- Customizable length (8-64 characters)
- Toggle uppercase letters, numbers, and symbols
- Password strength indicator
- One-click copy to clipboard
- Fully client-side (runs entirely in your browser)

## Getting Started

### Prerequisites

- Node.js (v16 or higher)
- npm or yarn

### Installation

1. Install dependencies:
```bash
npm install
```

2. Start the development server:
```bash
npm run dev
```

3. Open your browser and navigate to the URL shown in the terminal (usually `http://localhost:5173`)

### Build for Production

To create a production build:

```bash
npm run build
```

The built files will be in the `dist` directory. You can preview the production build with:

```bash
npm run preview
```

## Tech Stack

- React 18
- Vite
- Vanilla CSS

## License

See LICENSE file for details.
